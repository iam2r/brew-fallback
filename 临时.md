fix: pkgmap edge cases — git missing return, ver extraction without @, node@YY mapping
fix: replace OS_VER before VER in sed to avoid ver leaking
feat: auto-resolve runtime dependencies from MacPorts with install_name_tool
refactor: install deps into Cellar with brew-style layout + symlinks + autoremove
feat: pre-install Formula dependencies from brew info --json
feat: archive bottle fallback from ghcr.io + BREW_FALLBACK_PREFER strategy
chore: archive bottle cleanup/reset on failure + docs + KNOWN_ISSUES.md
